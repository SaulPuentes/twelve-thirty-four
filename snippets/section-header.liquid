{%- comment -%}
  Section Header
  Reusable heading + description + view-all link pattern

  Parameters:
  - heading: Section heading text (supports newlines via newline_to_br)
  - description: Description paragraph text
  - description_is_richtext: Boolean — when true, renders description in a <div> (for richtext settings that already include block-level HTML)
  - show_view_all: Boolean to show/hide the view-all link
  - view_all_link: URL for the view-all link
  - view_all_label: Label text for the view-all link (default: 'Explore the shop')

  Usage:
  {% render 'section-header',
    heading: section.settings.heading,
    description: section.settings.description,
    show_view_all: section.settings.show_view_all,
    view_all_link: section.settings.view_all_link,
    view_all_label: section.settings.view_all_label
  %}
{%- endcomment -%}

{%- if heading != blank -%}
  <h2 class="section-header__heading">{{ heading | newline_to_br }}</h2>
{%- endif -%}

<div class="section-header__bar">
  {%- if description != blank -%}
    {%- if description_is_richtext -%}
      <div class="section-header__description">{{ description }}</div>
    {%- else -%}
      <p class="section-header__description">{{ description }}</p>
    {%- endif -%}
  {%- endif -%}

  {%- if show_view_all and view_all_link != blank -%}
    <a href="{{ view_all_link }}" class="section-header__view-all">
      {% render 'animated-arrow' %}
      <span>{{ view_all_label | default: 'Explore the shop' }}</span>
    </a>
  {%- endif -%}
</div>

<style>
  .section-header__heading {
    font-family: var(--font-heading-family);
    font-size: 2.25rem;
    line-height: 1.1;
    margin: 0 0 1rem;
  }

  @media screen and (min-width: 1024px) {
    .section-header__heading {
      font-size: 3rem;
    }
  }

  .section-header__bar {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
    margin-bottom: 4rem;
    align-items: end;
  }

  @media screen and (min-width: 1024px) {
    .section-header__bar {
      grid-template-columns: 2fr 1fr;
    }
  }

  .section-header__description {
    font-family: var(--font-body-family);
    font-size: 1rem;
    line-height: 1.6;
    color: var(--color-foreground);
    opacity: 0.6;
    margin: 0;
    max-width: 630px;
  }

  @media screen and (min-width: 1024px) {
    .section-header__description {
      grid-column: 1 / 2;
    }
  }

  .section-header__view-all {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-body-family);
    font-size: 0.875rem;
    color: var(--color-foreground);
    text-decoration: none;
    transition: opacity 0.3s ease, color 0.3s ease;
    justify-self: start;
  }

  .section-header__view-all:hover {
    opacity: 0.7;
  }
</style>
